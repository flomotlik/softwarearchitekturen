<div id="left">
    <h2>Notifications</h2>
    <div id="notifications">
        <% for notification in @notifications %>
        <span class="notification"><%= notification.reason %></span>&nbsp;<% end %>
    </div>
	<%= @saved%>
	<%= @errors.full_messages%>
</div>
